using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyGun : MonoBehaviour {

    public GameObject EnemySeekerBullet;
    public float speedB = 6f;
    public GameObject bulletPrefabSeeker;
    public Transform bulletSpawn;
    // Use this for initialization

        /*
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

   // void FireEnemyBullet()
    {
        if(col)
       // GameObject playerpos = GameObject.Find("player");
        if(playerpos != null)
        {
           // GameObject bullet = (GameObject)Instantiate(EnemyBulletSeeker);
            GameObject bullet = (GameObject)Instantiate(bulletPrefabSeeker, bulletSpawn.position, bulletSpawn.rotation);
            bullet.GetComponent<Rigidbody2D>().AddForce(player.transform.position - transform.position).normalized * projectileSpeed;);



            bullet.transform.position = transform.position;

            Vector2 direction = playerShip.transform.position - bullet.transform.position;

         
       

            Destroy(bullet, 8f);
 }
    */
       





}
